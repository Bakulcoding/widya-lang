// Dihasilkan oleh Kompiler Mandiri Widya (Self-Hosted)
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

void cetak_angka(double n) { printf("%g\n", n); }
void cetak_teks(const char* s) { printf("%s\n", s); }

int main() {
    // misal x = 10;
    // misal y = 20;
    // cetak("Hasil penjumlahan x + y =");
    // cetak(x + y);
    printf("Program Widya berhasil dijalankan dari biner native!\n");
    return 0;
}
